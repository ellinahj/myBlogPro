import Layout from '../../src/components/common/Layout';
import AddContainer from '../../src/containers/list/AddContainer';

export default function add() {
  return (
    <Layout>
      <AddContainer />
    </Layout>
  );
}
